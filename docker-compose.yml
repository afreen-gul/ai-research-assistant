services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SERVE_FRONTEND: "true"
      FRONTEND_URL: "http://localhost:8000"
      PORT: "8000"
    volumes:
      - app-data:/app/backend/data
    restart: unless-stopped

volumes:
  app-data:
